name: 'issues archive'
description: 'issues archive'
author: 'zhengjitf'
inputs:
  token:
    required: true
    description: 'GITHUB TOKEN'
  repository:
    required: true
    description: 'The owner and repository name'
runs:
  using: 'node12'
  main: 'dist/index.js'
